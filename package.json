{
  "name": "ai-photo-animator",
  "version": "1.0.0",
  "description": "AI-powered photo animation with text-to-speech, lip syncing, and video generation",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["ai", "photo", "animation", "text-to-speech", "lip-sync", "video"],
  "author": "AI Photo Animator Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}